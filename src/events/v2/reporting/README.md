# Reporting Layer (Phase 5)

Read-only reporting helpers for the isolated V2 validation/scoring flow.

Scope:
- Build summary data objects from validation/scoring outputs
- Render lightweight markdown/text reports
- Stay decoupled from runtime/app state

Out of scope:
- CLI wiring in `package.json`
- UI rendering
- live game integration

