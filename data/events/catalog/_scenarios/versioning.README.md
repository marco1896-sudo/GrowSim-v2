# Scenario/Assertion Versioning

These files are **QA versioning metadata only** and **not loaded by runtime**.
They define scenario-set versions, assertion-set versions, and expected reviewed changes.
